[user]
	name = zeroicey
	email = zeroicey.hp@outlook.com
[safe]
	directory = *
{{ if eq .chezmoi.hostname "hpstation" }}
# hpstation 特有：GitHub 需走本地代理（mihomo/Clash 混合端口 7897）
[http]
	proxy = http://127.0.0.1:7897
[https]
	proxy = http://127.0.0.1:7897
{{ end }}
{{ if eq .chezmoi.hostname "hpcore" }}
# hpcore 特有：GitHub 认证走 gh auth
[credential "https://github.com"]
	helper =
	helper = !/usr/bin/gh auth git-credential
[credential "https://gist.github.com"]
	helper =
	helper = !/usr/bin/gh auth git-credential
{{ end }}
{{ if eq .chezmoi.hostname "iqibdl" }}
# iqibdl 特有：挂公共 WiFi，直连 GitHub 不通 → 所有 github.com 的 git 操作重写成自建 gh 代理
# （chezmoi update / lazy.nvim 插件 clone / mason registry 全走这里；
#  mason 的 release 下载另由 nvim 配置里的 download_url_template 重写）
[url "https://gh.zeroicey.me/https://github.com/"]
	insteadOf = https://github.com/
{{ end }}
